# Tests use the same H2 in-memory setup as the main application.properties.
# Override ddl-auto so Hibernate creates tables without needing schema.sql during tests.
spring.jpa.hibernate.ddl-auto=create-drop
spring.sql.init.mode=never

